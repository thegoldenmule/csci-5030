const assert = require('assert');

const parseSocketMessage = msg => JSON.parse(msg);

describe('Utilities', () => {
    describe('parseChatPayload', () => {
        it('creates chat record', () => {
            
        });
    });
});
